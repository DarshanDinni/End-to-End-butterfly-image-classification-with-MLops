from pathlib import Path

# Define file paths using the Path class from the pathlib module
CONFIG_FILE_PATH = Path("config/config.yaml")  # Path to the main configuration file
PARAMS_FILE_PATH = Path("params.yaml")          # Path to the parameters file
SECRET_FILE_PATH = Path("secrets.yaml")         # Path to the secrets file

